# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- main

pool:
  vmImage: 'windows-latest'

variables:
  serviceUrl: 'https://zukte.azurewebsites.net'
  buildConfiguration: 'Release'

steps:
- task: UseDotNet@2
  inputs:
    version: '5.0.x'
    performMultiLevelLookup: true

# requires CI pipeline to have protoc installed
# - task: PowerShell@2
#   inputs:
#     filePath: './protobuf.ps1'

- task: DotNetCoreCLI@2
  inputs:
    command: 'restore'
    projects: './aspnetapp/zukte.csproj'
    feedsToUse: 'select'

- task: DotNetCoreCLI@2
  inputs:
    command: 'build'
    projects: './aspnetapp/zukte.csproj'
    arguments: '--configuration $(buildConfiguration)'

- task: Cache@2
  inputs:
    key: 'npm | “$(Agent.OS)” | $(Build.SourcesDirectory)/zukte/client/package-lock.json'
    path: '$(Build.SourcesDirectory)/zukte/client/node_modules'
    cacheHitVar: 'CacheRestored'

- task: Npm@1
  inputs:
    command: 'install'
    workingDir: './client'
  condition: ne(variables['CacheRestored'], 'true')

- task: PowerShell@2
  inputs:
    filePath: './openapi.ps1'
    arguments: '$(pullRequest)'

- task: DotNetCoreCLI@2
  inputs:
    command: 'publish'
    projects: './aspnetapp/zukte.csproj'
    publishWebProjects: true
    arguments: '--configuration $(BuildConfiguration) --output $(Build.ArtifactStagingDirectory)'
    zipAfterPublish: True
    # workingDirectory: './aspnetapp'

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'zukte'
    publishLocation: 'Container'
