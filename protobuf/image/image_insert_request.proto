syntax = "proto3";
package Zukte.Message.Image;

/* represents a query to upload an image to blob storage */
message ImageInsertRequest {
  bytes image_data = 1;

  message ImageInsertResponse {
    string url = 1;
  }
}